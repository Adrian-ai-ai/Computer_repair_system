<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ $reportType }} Job Status Report</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 800px; margin: 0 auto; padding: 20px; }
        .header { background: #2563eb; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
        .content { background: #f8fafc; padding: 20px; border: 1px solid #e2e8f0; }
        .summary { background: white; padding: 15px; margin: 20px 0; border-radius: 6px; border-left: 4px solid #2563eb; }
        .job-card { background: white; margin: 10px 0; padding: 15px; border-radius: 6px; border: 1px solid #e2e8f0; }
        .status-badge { padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: bold; }
        .status-received { background: #fef3c7; color: #92400e; }
        .status-diagnosing { background: #dbeafe; color: #1e40af; }
        .status-waiting { background: #fef3c7; color: #92400e; }
        .status-repairing { background: #dbeafe; color: #1e40af; }
        .status-ready { background: #d1fae5; color: #065f46; }
        .status-delivered { background: #d1fae5; color: #065f46; }
        .footer { background: #f1f5f9; padding: 15px; border-radius: 0 0 8px 8px; font-size: 12px; color: #64748b; }
        table { width: 100%; border-collapse: collapse; margin: 15px 0; }
        th, td { padding: 8px 12px; text-align: left; border-bottom: 1px solid #e2e8f0; }
        th { background: #f8fafc; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{ ucfirst($reportType) }} Job Status Report</h1>
            <p>Generated on {{ now()->format('F j, Y \a\t g:i A') }}</p>
            @if($generatedBy)
                <p>Report generated by: {{ $generatedBy->name }}</p>
            @endif
        </div>

        <div class="content">
            @if($recipient && isset($recipient['type']))
                <div class="summary">
                    <h3>Report for {{ ucfirst($recipient['type']) }}</h3>
                    @if($recipient['type'] === 'client')
                        <p>Dear {{ $recipient['name'] ?? 'Valued Customer' }},</p>
                        <p>Here's the current status of your repair jobs:</p>
                    @else
                        <p>Here's the latest job status overview:</p>
                    @endif
                </div>
            @endif

            <div class="summary">
                <h3>Summary Statistics</h3>
                <ul>
                    <li><strong>Total Jobs:</strong> {{ $summary['total_jobs'] }}</li>
                    <li><strong>Completion Rate:</strong> {{ $summary['completion_rate'] }}%</li>
                    @if(isset($summary['jobs_in_period']))
                        <li><strong>Jobs in Period:</strong> {{ $summary['jobs_in_period'] }}</li>
                    @endif
                </ul>

                <h4>Status Breakdown:</h4>
                <ul>
                    @foreach($summary['status_breakdown'] as $status => $count)
                        <li>{{ $status }}: {{ $count }} jobs</li>
                    @endforeach
                </ul>
            </div>

            <h3>Job Details</h3>
            @if($jobs->count() > 0)
                @foreach($jobs as $job)
                    <div class="job-card">
                        <div style="display: flex; justify-content: space-between; align-items: start;">
                            <div>
                                <h4>{{ $job->job_number }}</h4>
                                <p><strong>Client:</strong> {{ $job->client->first_name }} {{ $job->client->last_name }}</p>
                                <p><strong>Device:</strong> {{ $job->device_type }} - {{ $job->brand }} {{ $job->model }}</p>
                                <p><strong>Fault:</strong> {{ $job->fault_description }}</p>
                                @if($job->receiver)
                                    <p><strong>Received by:</strong> {{ $job->receiver->name }}</p>
                                @endif
                            </div>
                            <div>
                                <span class="status-badge status-{{ strtolower(str_replace(' ', '-', $job->status)) }}">
                                    {{ $job->status }}
                                </span>
                                <p style="font-size: 12px; color: #64748b; margin-top: 5px;">
                                    {{ $job->created_at->format('M j, Y') }}
                                </p>
                            </div>
                        </div>

                        @if($job->statusHistory->count() > 0)
                            <div style="margin-top: 10px;">
                                <h5>Recent Status Changes:</h5>
                                <table>
                                    <thead>
                                        <tr>
                                            <th>Status</th>
                                            <th>Changed By</th>
                                            <th>Date</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach($job->statusHistory->take(3) as $history)
                                            <tr>
                                                <td>{{ $history->status }}</td>
                                                <td>{{ $history->user->name ?? 'Unknown' }}</td>
                                                <td>{{ $history->changed_at->format('M j, g:i A') }}</td>
                                            </tr>
                                        @endforeach
                                    </tbody>
                                </table>
                            </div>
                        @endif
                    </div>
                @endforeach
            @else
                <p>No jobs found for the selected criteria.</p>
            @endif
        </div>

        <div class="footer">
            <p>This report was automatically generated by the Inventory Repair System.</p>
            <p>Attachments: PDF report for printing + Excel file for data analysis.</p>
            <p>If you have any questions, please contact your system administrator.</p>
            @if($recipient && isset($recipient['type']) && $recipient['type'] === 'client')
                <p>Thank you for choosing our repair services!</p>
            @endif
        </div>
    </div>
</body>
</html>